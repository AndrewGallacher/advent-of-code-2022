export class Test {
  divisor: number;
  trueMonkey: number;
  falseMonkey: number;

  constructor(divisor: number, trueMonkey: number, falseMonkey: number) {
    this.divisor = divisor;
    this.trueMonkey = trueMonkey;
    this.falseMonkey = falseMonkey;
  }
}
